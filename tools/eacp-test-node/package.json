{
  "name": "eacp-test-node",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Node test runner for eacp WebView apps — drives them over the HTTP RPC bridge.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
